select city, country from country inner join city on city.country_id = country.id;
select payment_id, first_name, last_name from customer inner join payment on payment.customer_id = customer.id;
select rental_id, first_name, last_name from customer inner join rental on rental.customer_id = customer.id;
